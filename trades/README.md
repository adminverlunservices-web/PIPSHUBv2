# PIPSHUB Trades

Standalone Next.js manual trading interface used by the PIPSHUB Rise / Fall navigation.

## Run locally

```powershell
pnpm install
pnpm dev -- --port 3002
```

Open `http://localhost:3002`.

## Build

```powershell
pnpm build
```
